{{ 'collection-editorial.css' | asset_url | stylesheet_tag }}

<div class="collection-editorial container">
  {% if section.settings.title != blank %}
    <h1 class="collection-editorial-title">{{ section.settings.title }}</h1>
  {% endif %}

  <div class="collection-editorial-grid">
    {% for block in section.blocks %}
      {% case block.type %}
        {% when 'image' %}
          <div class="collection-editorial-item collection-editorial-item--{{ block.settings.size }}" {{ block.shopify_attributes }}>
            {% if block.settings.image %}
              <div class="collection-editorial-image">
                <img 
                  src="{{ block.settings.image | img_url: '800x' }}" 
                  alt="{{ block.settings.caption | escape }}"
                  loading="lazy">
              </div>
            {% else %}
              <div class="collection-editorial-image collection-editorial-image--placeholder"></div>
            {% endif %}
            {% if block.settings.caption != blank %}
              <p class="collection-editorial-caption">{{ block.settings.caption }}</p>
            {% endif %}
          </div>
        {% when 'text' %}
          <div class="collection-editorial-item collection-editorial-item--text collection-editorial-item--{{ block.settings.size }}" {{ block.shopify_attributes }}>
            <div class="collection-editorial-text">
              {{ block.settings.content }}
            </div>
          </div>
      {% endcase %}
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Collection Editorial",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "NEW COLLECTION"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption"
        },
        {
          "type": "select",
          "id": "size",
          "label": "Size",
          "options": [
            { "value": "small", "label": "Small" },
            { "value": "medium", "label": "Medium" },
            { "value": "large", "label": "Large" },
            { "value": "tall", "label": "Tall" }
          ],
          "default": "medium"
        }
      ]
    },
    {
      "type": "text",
      "name": "Text Block",
      "settings": [
        {
          "type": "richtext",
          "id": "content",
          "label": "Content"
        },
        {
          "type": "select",
          "id": "size",
          "label": "Size",
          "options": [
            { "value": "small", "label": "Small" },
            { "value": "medium", "label": "Medium" },
            { "value": "large", "label": "Large" }
          ],
          "default": "medium"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection Editorial"
    }
  ]
}
{% endschema %}

